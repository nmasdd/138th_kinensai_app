# その他事項 — 用語・データ素材・運用ルール

## 1. 用語

| 用語 | 定義 | 備考 |
| --- | --- | --- |
| 記念祭 | 東海中学校・高等学校の文化祭。毎年9月下旬開催 | 138th の開催日は 9/26(土) 12:00–16:00・9/27(日) 09:00–15:00、入場無料・予約不要 |
| テーマ | `Passione 〜一点に集い、未来の熱へ〜` | `festival.ts` に保持 |
| クラス企画 | 各クラスが行う出し物。中学はA〜I組、高校はA〜J組 | `kind: 'class'`。展示IDは組名（例：`1A`） |
| 教室有志企画 | 部活・組織が有志で行う出し物 | `kind: 'volunteer'`。IDは `vol-*` 形式 |
| 模擬店 | 飲食・屋台系企画。独立マスタはなく、正規表現 `/模擬店\|屋台\|フード\|軽食\|喫茶\|カフェ/` で判定する | 検索・マップの絞り込み専用 |

## 2. データ素材の置き場

| 素材 | 場所 | 読み手 |
| --- | --- | --- |
| 企画名・説明 | `kinensai_app/planning/{組}/title.txt, detail.txt` | `src/data/classContent.ts`（現在 1A／1B／1C のみ） |
| 講堂タイムテーブル | `kinensai_app/time/Auditorium.csv` | `src/data/timetable.ts`（4列 `team,day,start,end`、`day: 0=土,1=日`。5列ID付きも可） |
| ホーム文言 | `HP/`（`main.html`・`theme.html` 等） | `src/data/festival.ts` に抜粋済み。確定文言が来たら同ファイルだけ差し替える |
| 校内マップ原図 | `校内マップ/`（PDF／JPG。パンフレット由来で余白が大きい） | トリミング後に `kinensai_app/assets/maps/`（`f1.jpg, f2.jpg, f3.jpg, f45.jpg`）へ配置してから `map.tsx` で参照。Metro のバンドル外なので直接参照しない |
| タブアイコン | `kinensai_app/icon/` | `require('../../icon/...')` で参照（`assets/` ではない） |
| 138th 基本方針 | `基本方針.txt`（Shift-JIS のため直接読まず概要のみ参照） | テーマ `Passione` の根拠。文字化けするのでエディタで開くときは encoding 指定 |
| アプリ設定 | `kinensai_app/app.json`（`typedRoutes`・`reactCompiler` 有効） | 変更時は Expo Docs v56（https://docs.expo.dev/versions/v56.0.0/）を確認 |

## 3. 本データ到着前の差替機構（JSON ドロップイン）

ほとんどの詳細は未定のため、**情報を入れたらすぐ動く状態**を保つ。
下記 JSON を端末の `documentDirectory` に置くと同梱既定値を上書きできる
（Web では `documentDirectory` が falsy のため常に既定値）。形式は各 data
ファイル先頭の interface・コメントを正とする。

| ファイル | 形式例 | 既定値 |
| --- | --- | --- |
| `tickets.json` | `{"1A": {"required": "required", "time": "10:00〜"}}`（`required` は `none`／`required`） | 全企画 `unknown`（「確認中」表示） |
| `volunteers.json` | `Exhibition[]`（`id, className, projectName, description, ticketRequired, ticketTime, kind: 'volunteer', place`） | 仮2団体（有志A／B） |
| `stage-groups.json` | `[{id, name, detail}]` | 仮2団体（出演団体A／B） |
| `stage-votes.json` | `"group-a"`（投票先IDの文字列） | 未投票 |
| `congestion.json` | `{"level": "empty" \| "normal" \| "crowded"}` | `unknown`（「混雑状況: 確認中」） |
| `notifications.json` | `[{id, title, body, date}]` | 歓迎文1件（2026-09-04） |
| `favorites.json` | `["1A", "vol-1"]`（ID配列） | 空配列 |

## 4. 未確定時の表示フォールバック（統一文言）

- 企画名が空：`(タイトル未定)`（詳細モーダル）／`(タイトル)`（検索カード）
- 説明が空：`(説明準備中)`／`補足テキストがここに入ります。`
- 整理券・混雑・現在演目：`確認中` 系（`整理券: 確認中`／`混雑状況: 確認中`／`現在開催中の演目は確認中です`）
- 空一覧：講堂 `登録がありません`、マップ `一致する場所がありません`、
  検索 `一致する企画が見つかりません`、模擬店系 `模擬店の出店情報は準備中です`
- 画像未定：`M3ImagePlaceholder`（`image` アイコン）。`<Image>` 直置きや
  壊れ画像アイコンのまま放置しない。

## 5. プラットフォーム別注意

- Web（Metro dev）：txt／csv アセットの URI は相対パス
  （`/assets/?unstable_path=...`）で降ってくる。`fetch(uri)` で読むこと
  （`classContent.ts`・`timetable.ts` に実装済み）。
  `expo-file-system` の読取系・`documentDirectory` 依存は Web で動かない。
- `metro.config.js` の `config.resolver.assetExts.push('csv', 'txt')` は必須。
  消すと txt／csv がバンドルされず全企画・講堂が 0 件になる。
- ファイルIOは `expo-file-system/legacy` を使う（新 API ではない）。
- `ToastAndroid`・`expo-camera` 等のネイティブ専用 API を呼ぶ箇所は、
  Web／iOS での代替・ガードを必ず用意する。
- `.expo/types/router.d.ts` は生成物（`expo start` で再生成）。
  `camera.tsx` の `/map` 遷移にある `as never` キャストは再生成までの暫定。

## 6. 作業ルール

- コマンドは `kinensai_app/` で実行する。ルート直下の `package.json` は
  古い・冗長なため使わない。
- 検証は `node_modules\.bin\tsc --noEmit` と
  `node_modules\.bin\eslint src\ --quiet`（テストランナーなし）。
- Playwright MCP の既定 viewport は 390×844。
  `.playwright-mcp/`（スクショ・ログ）、`android/`（prebuild 出力）は
  コミットしない。
- `仕様書.md` にない無関係機能は実装しない（既存なら削除）。
  関連する未記載機能は `仕様書.md` に追記すれば許可不要で実装可。
  情報不足時はまず `HP/` のソースを見ること。
