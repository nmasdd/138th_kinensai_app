# デザインルール — Material 3 Expressive

本アプリのUIは Material 3 Expressive（M3）に準拠する。
色・タイポグラフィ・形状の定義は `kinensai_app/src/theme.ts`、
実装コンポーネントは `kinensai_app/src/components/m3.tsx` に集約する。
この2ファイル以外に色・サイズのハードコードを書かない。

## 1. カラースキーム（カスタムライト、固定）

`theme.ts` の `m3` オブジェクトが唯一の正本。`as const` のため直接書き換えず、
追加が必要な場合はロール名を決めてここに追記してから使う。
ライトモード固定（`userInterfaceStyle` に依存しない）。旧 `theme` エイリアスは
互換用であり、新規コードは `m3` ロールを直接参照すること。

| ロール | 値 | 用途 |
| --- | --- | --- |
| `primary` | `#D14E00` | ブランド色。主要ボタン・FAB・選択中タブインジケータ・ヒーロー背景 |
| `onPrimary` | `#FFFFFF` | `primary` 上の文字・アイコン |
| `primaryContainer` | `#FFDBCA` | FAB・リストアイコン背景・`onPrimaryContainer` と組で使う |
| `onPrimaryContainer` | `#331100` | `primaryContainer` 上の文字・アイコン |
| `secondaryContainer` | `#FED8B7` | 選択中チップ・バッジ・ナビゲーションピル・トーナルボタン背景 |
| `onSecondaryContainer` | `#2F1D00` | `secondaryContainer` 上の文字・アイコン |
| `tertiaryContainer` | `#FFE3C2` | 予備（現状未使用に近い。使う場合はここに用途を記録する） |
| `surface` | `#FEF8F4` | 画面背景。全画面の `SafeAreaView` はこの色 |
| `surfaceContainerLow` | `#F8F3EE` | elevated カード・連結リスト項目 |
| `surfaceContainer` | `#F2EDE8` | タブバー背景 |
| `surfaceContainerHigh` | `#ECE7E3` | 検索バー・タイムボックス・詳細ボックス |
| `surfaceContainerHighest` | `#E7E2DD` | filled カード・画像プレースホルダ |
| `onSurface` | `#1E1B18` | 本文・見出しの既定色 |
| `onSurfaceVariant` | `#4E453C` | 補足文・プレースホルダ文字・非選択ラベル |
| `outline` | `#7F756C` | 非選択チップ枠 |
| `outlineVariant` | `#D0C5BA` | カード枠・区切り線・タブ下線 |
| `inverseSurface` | `#33302C` | カメラプレビュー・未許可パネルの背景 |
| `inverseOnSurface` | `#F5F0EB` | `inverseSurface` 上のアイコン・文字 |
| `inversePrimary` | `#FFB68F` | カメラの読み取り枠線 |
| `error` / `onError` | `#B3261E` / `#FFFFFF` | エラー表示 |
| `errorContainer` / `onErrorContainer` | `#F9DEDC` / `#410E0B` | エラー面 |

面の段差は一段ずつ上げる（例：画面 `surface` → ボックス `surfaceContainerHigh` →
カード `surfaceContainerHighest`）。段差を2段以上飛ばさない。
青 `#208AEF` は旧色であり、新規UIでの使用を禁止する
（スプラッシュ等の残存分は見つけ次第 M3 ロールに置き換える）。

## 2. タイポグラフィ

`m3type` のみ使う。行間はサイズの約1.4倍で定義済み。
Android のシステム書体は Roboto のため `fontFamily` は指定しない。

| トークン | size / weight / lineHeight | 主な用途 |
| --- | --- | --- |
| `displaySmall` | 36 / 400 / 44 | ホームの「138th 記念祭」 |
| `headlineMedium` | 28 / 400 / 36 | 「ステージ」「講堂」等のセクション見出し |
| `headlineSmall` | 24 / 400 / 32 | 「土曜日」「日曜日」等の日別見出し |
| `titleLarge` | 22 / 400 / 28 | TopAppBar のタイトル |
| `titleMedium` | 16 / 500 / 24 | カード見出し・本文見出し |
| `titleSmall` | 14 / 500 / 20 | タブ文字・補足見出し |
| `bodyLarge` | 16 / 400 / 24 | 本文 |
| `bodyMedium` | 14 / 400 / 20 | 補足文・結果件数 |
| `labelLarge` | 14 / 500 / 20 | ボタン・チップ・場所バッジ（ボタン内は 15px に上書き） |
| `labelMedium` | 12 / 500 / 16 | 日付・チケット行・タブバーラベル（タブバー内は 11px に上書き） |

見出しのサイズ直書き（例：`fontSize: 35`）は禁止。必ず上のトークンに寄せる。

## 3. 形状・影・モーション

形状は `m3shape` のみ使う：`pill: 999`（ボタン・検索バー・チップ・バッジ・
ナビゲーションピル）、`card: 20`（カード・画像・マップ）、`dialog: 28`
（ダイアログ・連結リストの外端）、`fab: 16`／`fabLarge: 28`／`fabSmall: 12`。

- 影は elevated カードのみ：`elevation: 1`＋`boxShadow: '0 1px 3px rgba(0,0,0,0.12)'`。
  FAB・閉じるボタンは `elevation: 3`＋`'0 2px 6px rgba(0,0,0,0.3)'`。
  それ以外の面に影を付けない。
- タップ面は `M3Touch` を使う：`android_ripple: rgba(0,0,0,0.12)`、
  押下時スケール 0.97（90ms）→ 復帰（120ms、`useNativeDriver`）。
  `round` 指定時は `pill` でクリップする。

## 4. コンポーネント規格（`m3.tsx`）

- `TopAppBar`：高さ 64、`paddingHorizontal: 4`、背景 `surface`。
  左 48×48（角丸 24）の menu→`/menu`、中央 `titleLarge` 中央寄せ、
  右 48×48 の notifications→`/notifications`。
- `M3Button`：高さ 56・ピル型、`paddingHorizontal: 16`、`gap: 8`、
  アイコン 20px、ラベルは `labelLarge` を 15px 上書き・1行省略可
  （`numberOfLines={1}`＋`flexShrink: 1`）。
  `filled`（`primary`／`onPrimary`、主要アクション）、
  `tonal`（`secondaryContainer`／`onSecondaryContainer`、準主要）、
  `outlined`（透明＋1px `outline` 枠）。横並び時は親で `flex: 1, minWidth: 0` を付け、
  390px 幅でもはみ出さないこと。
- `M3SearchBar`：高さ 56・ピル型、背景 `surfaceContainerHigh`、
  `paddingHorizontal: 16`、`gap: 4`、入力文字 16px。
  先頭 `search` アイコン固定、末尾は入力時 `clear`（全消去）・空欄時 `mic`。
  音声検索は未実装のため、押下時は Android のみトースト表示し、
  Web／iOS では何もしない（`Platform.OS` ガード必須、`ToastAndroid` 直呼び禁止）。
- `M3Card`：角丸 `card`（20）、内側 `padding: 16`、`overflow: hidden`。
  `filled`＝`surfaceContainerHighest`、`elevated`＝`surfaceContainerLow`＋上記影、
  `outlined`＝`surface`＋1px `outlineVariant` 枠。
  画像領域は `M3ImagePlaceholder`（`surfaceContainerHighest`、
  `image` アイコン 40px・`onSurfaceVariant`、既定高さ 140）で仮置きし、
  `<Image>` を直接置かない。
- `M3FAB`：トーナル（`primaryContainer` 面＋`onPrimaryContainer` アイコン）、
  56px（small 40px）、角丸 `fab`（16）／small は 12。画面端から 16dp に絶対配置。
- `M3IconButton`：48px 円形。選択中は `primaryContainer`、非選択は
  `secondaryContainer`。
- `M3PrimaryTabs`：高さ 48、`borderBottomWidth: 1`（`outlineVariant`）。
  選択中のみ文字 `primary`（`titleSmall`）、3px 高・48px 幅・上端角丸 3 の
  インジケータを表示。非選択は `onSurfaceVariant`。
- `M3ListItem`：最小高さ 72、背景 `surfaceContainerLow`、
  `paddingHorizontal: 16`／`paddingVertical: 8`、`gap: 16`、間隔 3。
  先頭 40px 円形アイコン（`primaryContainer` 面）。
  連結時は `position`（`top`／`middle`／`bottom`／`single`）で形状指定：
  外端 28・内端 8。
- `M3NavBar`（タブバー、`src/app/(tabs)/_layout.tsx`）：`minHeight: 80`、
  `paddingTop: 8`＋セーフエリア下余白、背景 `surfaceContainer`、横5等分。
  選択中は 64×32 のピル型インジケータ（`secondaryContainer`、
  アイコン色 `onSecondaryContainer`）＋ラベル（`onSurface`・700）。
  非選択はアイコン・ラベルとも `onSurfaceVariant`。
  順序は カメラ（`qr-code-2`）→ タイムテーブル（`calendar-month`）→
  ホーム（`home`）→ 検索（`search`）→ マップ（`map`）で固定。
- アイコンは `@expo/vector-icons` の `MaterialIcons` のみ使う
  （`M3Icon` 経由、サイズ既定 24px）。

## 5. 画面雛形・レイアウト数値

- 画面は `SafeAreaView（edges={['top']}）`＋`TopAppBar`＋内容の構成。
  コンテナは `flex: 1`・背景 `surface`。
- 本文は `padding: 16`・要素間 `gap: 12`・下余白 24
  （FAB・下部固定検索がある画面は 96）。
- ヒーロー（ホーム）は `primary` 面・角丸 20・`padding: 20`、文字は `onPrimary`。
- 画像・マップは幅 380・`maxWidth: '100%'`・高さ 380・角丸 20・中央寄せ。
- 通知・スタック画面の「戻る」ボタンは右下（`alignItems: flex-end`、
  `paddingHorizontal: 16, paddingBottom: 16`）。
- `src/app/` 下にはルートファイルのみ置く。共有UIは `src/components/`、
  ルート外のロジック・データは `src/data/`・`src/theme.ts` に置く。

## 6. 追加・変更時の手順

1. 新しい色・サイズ・角丸が必要になったら、まず `theme.ts` に
   ロール／タイプ／形状として追加する（ハードコードのまま使わない）。
2. 新しいUIパターンは `m3.tsx` にコンポーネントとして切り出し、
   高さ・余白・色の対応をこのファイルに記録する。
3. 390px 幅（Playwright MCP の既定 viewport）で全タブ＋通知の
   スクリーンショットを確認し、はみ出し・省略・0件表示がないことを確かめる。
