# 仕様書 — 138th 記念祭アプリ（Expo SDK 56 / expo-router）

## 1. 構成概要

- スタック：Expo SDK 56／React Native 0.85／React 19／TypeScript strict／
  expo-router（file-based）。入口は `kinensai_app/src/app/_layout.tsx`（Stack）。
- `app.json` の `experiments` で `typedRoutes` と `reactCompiler` を有効化。
  パスエイリアス `@/*` → `src/*`（`tsconfig.json`）。
- Web は static 出力（`expo start --web`、開発時 `http://localhost:8081`）。
- **予約機能は仕様上存在しない。** 予約タブ・予約Context・予約用CSV／JSONを
  追加してはならない。既存を見つけたら削除する。
- **デザインは Google Material Design 3（Material 3 Expressive）に準拠する。**
  **アニメーション／モーションは Apple Human Interface Guidelines (HIG) に準拠する。**
  詳細は `デザインルール.md`、色・タイポグラフィ・形状の定義は
  `src/theme.ts`、UI 部品は `src/components/m3.tsx`、モーションは
  `src/components/anim.tsx` に集約する。

## 2. タブバー（`src/app/(tabs)/_layout.tsx`）

5タブをこの順序で固定する。中央が初期ルート。

| 順序 | ルート | ラベル | アイコン |
| --- | --- | --- | --- |
| 1 | `camera` | カメラ | `qr-code-2` |
| 2 | `timetable` | タイムテーブル | `calendar-month` |
| 3 | `index` | ホーム | `home`（`initialRouteName`） |
| 4 | `search` | 検索 | `search` |
| 5 | `map` | 校内マップ | `map` |

カスタム `M3NavBar`（高さ 80・`surfaceContainer`、選択中は 64×32 の
`secondaryContainer` ピル＋ラベル強調）を使用し、標準タブバーは使わない。

## 3. カメラ（`(tabs)/camera.tsx`、expo-camera）

- 廊下のQRコード読取専用。`barcodeScannerSettings: { barcodeTypes: ['qr'] }`、
  背面カメラ、読取枠オーバーレイ付きプレビュー。
- 権限状態で分岐：`null`（取得中）→ `ActivityIndicator`、
  未許可 → 説明文＋「カメラを許可する」ボタン、
  許可済 → プレビュー。現在地QR以外を読取った場合は結果カード＋
  「マップで関連企画を見る」（`router.push({ pathname: '/map', params: { loc: scanned } })`）＋
  「もう一度読み取る」（状態リセット）。二重読取ガードあり。
- 現在地QR（`data/locationQr.ts` の `parseLocationQr`）：読み取った値が
  `/map?...&x=..&y=..` を含む場合、確認を挟まず**即座に** `floor`/`x`/`y` を付けて
  `/map` へ自動遷移する（マップ側で青い現在地ドットを表示）。遷移後はカメラを
  停止し、再入場時は結果カードから「もう一度読み取る」で再開する。
  `/map` を含まない場合は従来どおり結果カードを表示する。

## 4. タイムテーブル（`(tabs)/timetable.tsx`）

Google カレンダー風の時間グリッド + Material 3 で構成する。講堂とステージの
2会場を、同じ時間グリッドで日別に表示する。

- 会場切替（最上位）：`M3PrimaryTabs`「ステージ／講堂」（`VENUE_LABELS`）。
  選んだ会場のカレンダーだけを表示する（初期値はステージ）。
- 日切替：その下に `M3PrimaryTabs`「土曜日／日曜日」。初期値は当日の曜日
  （開催日以外は土曜）。催し時間は `festival.dates` から取得。
- ステージ：見出し「ステージ」（`headlineMedium`）＋トーナルボタン
  「オーディエンス投票」（→ `/vote`）。出演団体のうち曜日・開始・終了が
  確定しているものを時間グリッドに表示（`groupStageItems`）。未確定の
  団体は一覧のみ。横幅 390px でも省略されないよう見出し側 `flexShrink: 1`・
  ボタン側 `flex: 1, minWidth: 0`。
- 講堂：見出し「講堂 音楽祭／海神」の隣に、オーディエンス投票ボタンと同程度の
  大きさの混雑チップ（`loadCongestion`、empty=secondary / normal=tertiary /
  crowded=error / unknown=surface の M3 ロールで色分け）。`loadAuditorium` の
  演目を時間グリッドに表示（日付・催し時間の行は置かない）。
- 時間グリッド（1分=1.2dp）：左ガターに毎時の時刻、横罫線、演目を
  開始/終了に合わせたブロックで配置。重なりは列分割して横並び。
  遅延は赤バッジ、開催中は `tertiaryContainer`、現在時刻は赤線＋ドット。
  表示範囲は催し時間（土 12:00–16:00 / 日 09:00–15:00）を基準に、
  演目がはみ出す分だけ拡張する。演目0件は「◯◯の演目は準備中です」。
- 出演団体：ステージは `usePerformerGroups('stage')`（`stage-groups.json`）、
  講堂は `usePerformerGroups('auditorium')`（`auditorium-groups.json`）の
  一覧をカード表示（画像 or アイコン＋団体名＋短い紹介＋chevron）。
  ステージ・講堂の両タブに表示する（土日共通）。講堂の一覧は
  `time/Auditorium.csv` の演目名を正とし、`auditorium-groups.json` の
  メタ情報（紹介文・写真）を `auditoriumGroupsForItems` で突き合わせる。
  カードをタップすると `PerformerDetailModal`（ボトムシート状モーダル）で
  写真（なければ `groups` アイコンのプレースホルダ）・ジャンルチップ・
  短い紹介・長い紹介文（`intro`。未設定なら短い紹介）を表示する。
- 講堂データ：`time/Auditorium.csv`。4列 `team,day,start,end`
  （`day: 0=土, 1=日`）、5列（先頭ID付き）も受理。不正行は捨てる。
  `delayMinutes` は既定 0（リアルタイム遅延配信の受口）。
- ステージデータ：`stage-groups.json`（同梱既定は `data/stage.ts` の
  `bundledGroups`）。各団体が任意で `day`/`start`/`end`/`delayMinutes` を持つ。
  管理者ページ・ステージ/講堂の団体フォームから入力する。
  現在は動作確認用サンプルを同梱している。
- 講堂出演団体データ：`auditorium-groups.json`（同梱既定は
  `bundledAuditoriumGroups`）。紹介文 `intro`・ジャンル `genre`・写真
  `imageUri` を持つ。時間割は CSV を正とするため `day`/`start`/`end` は持たない。

## 5. ホーム（`(tabs)/index.tsx`）

- ヒーロー（`primary` 面・角丸 20）：`festival.ts` の学校名・祭名・
  テーマ・日程・入場条件を表示。
- クイック行：トーナルボタン「オーディエンス投票」（→ `/vote`）・
  「パンフレット」（→ `/pamphlet`）を横2列。各 `flex: 1, minWidth: 0`。
- 「近道」カード（outlined＋`QuickNav`）：マップ／検索／タイムテーブル／
  カメラ／投票／パンフ／通知／模擬店へ各1タップ（ホームから全ページ2タップ以内）。
- 「いま開催中」カード（elevated）：講堂の現在演目、なければ確認中文。
  ローディングは `M3LoadingView`、件数・結果はライブリージョン。
- 「おすすめ企画（ランダム2件）」カード（filled）：管理者おすすめ
  （`picks.json`）があればそれを対象に、未設定なら `loadAllExhibitions` の
  全件から、表示のたびに `sampleRandom` で2件を抽選。バッジ＋企画名＋
  説明2行。区切りは `M3Divider`。0件時は「おすすめ企画は準備中です」。
- 「アクセス」「来場のお願い」「記念祭について」カード（outlined）：
  `festival.ts` の住所・交通・注意書き・概要文。

## 6. 検索（`(tabs)/search.tsx`）

- ソースは `loadAllExhibitions()`（クラス企画＋有志企画）。
  失敗時は空配列にフォールバックし、ローディング中は `ActivityIndicator`。
- 検索バー（`M3SearchBar`）のクエリで `className`／`projectName`／
  `description` を部分一致（小文字化）。件数表示は
  クエリあり「検索結果: {n}件」・なし「全 {n} 件の企画」。
- 絞り込み（FAB「絞り込み」で開閉、`filter` パラメータで初期指定可）：
  すべて／クラス企画／有志企画／模擬店／お気に入り。
  模擬店は正規表現 `/模擬店|屋台|フード|軽食|喫茶|カフェ/` で判定。
  模擬店0件時は「模擬店の出店情報は準備中です」、
  それ以外は「一致する企画が見つかりません」。
- カードタップで `ExhibitionDetailModal`（ボトムシート状モーダル）：
  画像プレースホルダ・組名・企画名（空なら「(タイトル未定)」）・
  説明（空なら「(説明準備中)」）・整理券行・場所行・
  お気に入り登録／解除ボタン（`★お気に入り` 表示連動）。
  検索からは追加で「マップを開く」ボタン（`showMapButton`）を表示し、
  `/map?focus={id}` へ遷移して該当企画を選択表示＋その階へ切替する。
- 整理券表記（`ticketLabel`）：`none`→「整理券: 不要」、
  `required`→「整理券: 必要（配布時間があれば付記）」、
  `unknown`→「整理券: 確認中」。
- お気に入りは `useFavorites`（`favorites.json` 永続化、ID配列）。
  検索と校内マップで状態を共有するため、`data/favorites.ts` は
  モジュールレベルのストア + `useSyncExternalStore` で全画面同期する
  （永続化は kvStore 経由。Web は localStorage、ネイティブは documentDirectory）。

## 7. マップ（`(tabs)/map.tsx`＋`components/VectorMapView.tsx`＋`data/vectorMap.ts`）

- 上段タブ（`M3PrimaryTabs`）：1階／2階／3階／4階 5階／模擬店。
- 地図は画像埋め込みなしのベクター模式図（Googleマップ風）。
  原図（`校内マップ/*.jpg`）を元に `vectorMap.ts` へ部屋配置を起こし、
  `VectorMapView` が描画する。`assets/maps/*.jpg`・`<Image>` は使わない。
  管理者は `/admin/map` から保存した `map-layout.json`（フロアごとの部屋配列）で
  配置を上書きできる。上書きがない階は同梱の `vectorMap.ts` を使う。
- 操作は Googleマップ同様：ドラッグでパン、ピンチ／ホイール／ダブルタップ相当
  （＋−ボタン）でズーム（1x〜4x）。**ズームはジェスチャーした位置を基準**に
  拡大縮小する（カーソル下・ピンチ中点・ダブルタップ位置のワールド点を固定。
  ＋−ボタンは画面中央基準）。操作ボタン・スケール表示（階・倍率）・
   凡例（教室／特別教室／屋外）は地図と重ならないよう地図の外（下）に配置する。
   フロア切替は短いクロスフェードで再描画する（`ScreenFade`、Reduce Motion 時は即時）。
- 部屋タップで詳細ボックスに反映（モーダルは開かない）。
  選択部屋は `primaryContainer` 強調＋ピン表示（点滅はしない）。
- QR遷移（`loc` パラメータ）あり→バナー「QR読取位置: {組名}付近／原文」、
  一致企画があれば詳細ボックスに preselect＋現在地マーカー。
- 企画フォーカス（`focus` パラメータ、検索の詳細「マップを開く」から）：
  指定企画を詳細ボックスに preselect し、その企画の階へ自動切替（バナーなし）。
- URL現在地（`floor`・`x`・`y` パラメータ、例 `/map?floor=2&x=0.5&y=0.3`）：
  指定階へ自動切替し、青いドット（白縁＋半透明の精度円）を表示して初回のみ
  中央へ寄せる。`x`/`y` は 0〜1 で相対座標、1超は 1000x700 のワールド座標として
  解釈する。`floor` は 1〜5
  （4・5 は「4階 5階」）。指定階以外ではドットを描画しない。
  バナー「現在地: {階} 付近」＋「現在地へ移動」ボタン（`my-location`）付き。
- 詳細ボックス（`surfaceContainerHigh`・角丸 28・最小高 200）：
  未選択時は「一覧から場所を選ぶとここに詳細を表示します。」、
  選択時は組名・企画名・説明3行＋同居企画リンク＋「詳細を見る」（モーダルを開く）。
- 企画リスト：上に「お気に入り企画」、下に「企画一覧」の2セクション。
  お気に入りは登録済みの企画（0件時は「お気に入りに登録した企画がここに
  表示されます」）。各行はバッジ＋企画名＋（お気に入りは★）＋chevron。
  検索クエリで両セクションを絞り込み、0件時は「一致する企画がありません」。
- 下部固定検索バーで `className`／`projectName`／`place` を絞り込み。

## 8. 通知（`notifications.tsx`＋`notifications/[id].tsx`）

- 実行委員会からのお知らせ一覧。カード（elevated＋画像プレースホルダ高さ140）：
  日付（`labelMedium`）・タイトル・本文2行＋「詳細を開く」。
  タップで `/notifications/{id}` に遷移し全文表示。空なら「通知はありません」。
- 右下「前の画面に戻る」ボタン（`router.back()`）。
- ローディング中も `TopAppBar`「通知」を表示する。
- データは `loadNotifications()`：同梱の歓迎文1件＋
  `notifications.json`（`{id, title, body, date}[]`）差替可。

## 9. メニュー（`menu.tsx`、左から来る想定のスタック画面）

- 全画面共通の TopAppBar 左 menu から `router.push('/menu')` で開く。
- 連結 `M3ListItem` 9件：ホーム（`/`）／タイムテーブル／カメラ／検索／
  校内マップ／模擬店（`/search?filter=mogiten`）／オーディエンス投票／
  パンフレット／通知。各 `router.replace` で遷移。
- 右下の円形閉じるボタン（56px・`primary`）で `router.back()`。

## 10. 投票（`vote.tsx`）・パンフレット（`pamphlet.tsx`）

- 投票：出演者検索バー＋団体カード一覧。カードまたはハートボタンで
  1団体に投票（変更可、`stage-votes.json` に端末保存、投票中は
  `M3Badge`「投票中」＋塗りハート）。0件時は「一致する出演者がいません」。
  右下「前の画面に戻る」ボタン（`router.back()`）。
- パンフレット：データ未着のため「まだデータがない」＋説明文＋
  「ホーム」ボタン（`router.replace('/')`）。
  データ形式確定後に本実装する。

## 11. データ層（`src/data/`）と Web 対応

- 同梱の共有コンテンツ正本は `src/data/bundled/*.json` に集約する
  （`volunteers.json`＝有志企画45件、`stage-groups.json`＝ステージ出演団体、
  `auditorium-groups.json`＝講堂出演団体、`class-catalog.json`＝クラス企画38件）。
  各 `src/data/*.ts` は JSON を import するだけにし、TS リテラルを増やさない
  （差分レビュー・一括編集・バリデーションのため）。
- クラス企画：`bundled/class-catalog.json` を唯一の正本とする
  （`classContent.ts`。旧 `planning/{組}/title.txt, detail.txt` の読込は廃止）。
  組追加は JSON にエントリを足すだけ。中学A〜I・高校A〜J を想定し
  ハードコードの一覧を持たない。
- **Web では `asset.localUri` が相対パス
  （`/assets/?unstable_path=...`）になり、かつ `expo-file-system` の
  読取系は未対応のため、`Platform.OS === 'web'` の場合は `fetch(uri)`
  （相対URLは同一オリジン解決）で読む。ネイティブは従来通り FileSystem。
  この分岐は `timetable.ts` に実装済み（クラス企画は同梱JSONのため対象外）。
  同理由で `metro.config.js` の `assetExts` への `csv, txt` 追加を消さないこと。
- `FileSystem.documentDirectory` は Web で falsy のため、JSON 差替系
  （tickets／volunteers／stage-groups／auditorium-groups／stage-votes／
  congestion／notifications）は Web では同梱既定値を返す。ただし `favorites` は
  kvStore 経由（Web は localStorage）に変更済みで Web でも永続する。
  ファイル参照パスをモジュール頂部で組み立てる実装は変えないこと
  （ガード節が使用前に弾く前提）。
- 公開前データの構造検証は `data/validateContent.ts`。`/admin/data` の
  「公開前チェック」と公開処理の直前で呼び、エラーがあれば公開を中止する。

## 12. 禁止・必須ルール

- 予約関連（タブ・Context・`reservation/`・予約CSV／JSON・予約UI）を
  新規作成しない。既存を見つけたら削除する。
- `src/app/` 下にはルートのみ置く（`Header` 等の共有UIを置くと
  ルートとして誤登録される）。共有UIは `src/components/`、
  型・データ・フックは `src/data/`・`src/theme.ts`。
- 新規UIで青 `#208AEF` を使わない。色・サイズ直書きをしない。
- `ToastAndroid` は Android 専用のため、呼ぶ場合は必ず
  `Platform.OS === 'android'` ガードを付ける。
- 未確定情報の表示文言は統一する：タイトル「(タイトル未定)」、
  説明「(説明準備中)」／「補足テキストがここに入ります。」、
  整理券・混雑「確認中」、空一覧「登録がありません」系。

## 13. 全世界公開フロー（管理者→全端末）

- 共有コンテンツの読込優先度（`data/kvStore.ts`）：リモート配信 ＞
  開発ビルドの端末上書き（プレビュー）＞ 同梱の正本。
  お気に入り・投票は端末個人データとして従来通り端末保存する。
- **開発と本番の分離**（`data/remoteConfig.ts`）：開発時（`__DEV__`）は
  既定でリモート取得を**行わない**（同梱値のみ。本番KVへの接続エラーを出さない）。
  配信値を確認したいときだけ `EXPO_PUBLIC_ENABLE_REMOTE_CONTENT=1`、
  配信先を変えるときは `EXPO_PUBLIC_CONTENT_URL` を付けて起動する。
  本番ビルドでは常に有効。
- 配信基盤：`app.json` の `extra.contentUrl` 配下の同名 `*.json` を取得
  （8秒タイムアウト・失敗時同梱値）。`GET /api/content/_meta.json` が公開の
  版数 `{ version, updatedAt }` を返し、各キーの取得URLに `?v=<version>` を付ける。
- **反映遅延は30秒以内**：Worker の `cache-control` は
  `max-age=15, stale-while-revalidate=15`（`_meta.json` は10秒）。クライアントは
  起動時・フォアグラウンド復帰時・在席中は15秒ごとに `_meta.json` を確認し、
  版数が変わればメモリキャッシュを破棄して購読中の画面（`useContentEffect`）へ
  再読込を通知する。復帰時は即時反映。
- 公開手順：`/admin/*` で編集→この端末でプレビュー→
  `/admin/data` の「公開前チェック」（構造検証。エラーがあると公開不可）→
  「全世界に公開」で全端末へ配信。公開すると Worker が `_meta.json` の版数を
  更新する。「プレビュー破棄」でこの端末の編集を取り消す。
  「配信中の値を確認」で本番KVの実値（他端末が見る値）を取得表示できる。
  「公開バンドル書き出し」はバックアップ・目視確認用。
- `/admin/stage`（ステージ/講堂）：会場切替（ステージ／講堂）で出演団体を
  追加・編集・削除する。団体は団体名・ジャンル・短い紹介・詳細な紹介文
  (`intro`)・画像を持つ。時刻・曜日・遅延はステージのみ入力する（講堂の
  時間割は「講堂タイムテーブル」＝`time/Auditorium.csv` を正とする）。
- 管理者ページは直接アクセス専用（タブ・メニューに入口なし）。
  `/admin/index` 先頭の案内バナーでプレビュー／公開の区別を明示する。
- `/admin/qr`（現在地QRコード作成）：フロア地図（`VectorMapView` の作成モード
  `onPick`）をタップして地点を指定し、`https://app.kinensai.jp/map?floor=..&x=..&y=..`
  を `QrCodeView`（`toqr` で View グリッド描画）で表示する。印刷して廊下などに貼る。
  指定地点は青い現在地ドットで地図上に表示される。
- `/admin/map`（マップ配置）：フロア地図で部屋をタップ/一覧選択し、地図タップで
  選択中の部屋を移動、`X`/`Y`/`幅`/`高さ` の数値入力と nudge (←→↑↓・10ずつ)、
  ラベル・名前・種類 (教室/廊下/階段等)、部屋の追加・削除を編集する。
  **開発サーバー (Metro) 接続時は「配置を保存」で `src/data/vectorMap.ts` の
  `VECTOR_ROOMS` を直接書き換える**（`metro.config.js` の dev ミドルウェア
  `POST /__admin/write-map`。全階の現在値で生成し直し、端末上書きは解除）。
  開発サーバーが無い場合は端末プレビュー `map-layout.json` に保存し、
  「データ管理」の公開で全端末に反映する。「この階を既定に戻す」で同梱値に戻す。

## 14. アニメーション（`components/anim.tsx`）

- **本アプリのアニメーション／モーションは Apple Human Interface Guidelines
  (HIG) に準拠する。** HIG に沿った**控えめなモーション**を使う。
  目的は状態変化の伝達と操作フィードバックで、装飾のための動きは足さない。
  動きは短く（出現 260ms 前後）、自然なスプリングで終える。
- OS の「視差効果を減らす」(Reduce Motion) を尊重する。有効時は移動・拡大を
  止め、フェードのみ（または即時表示）にする。`useReducedMotion()` を全
  アニメーションで参照する。
- `components/anim.tsx` の役割（React Native Reanimated 実装）:
  - `ScreenFade`：画面/フロア切替。フェード＋ごく僅かな拡大。
  - `Rise` / `Stagger`：出現時に下から少しせり上げる。`delay`／`index` で
    一覧を順に表示（`index` 由来の遅延は最大 280ms）。
  - `Pop` / `ConfirmPop` / `TabPop`：モーダル内容・確定バッジのポップ。
  - `FadeOverlay`：モーダル暗幕のフェード。
  - `Pulse`：注意喚起のゆっくりした明滅。
  - `ScanBeam`：QR ファインダー内を上下する走査ライン。
  - `SuccessCheck`：成功バッジのポップ。`SkeletonBox`/`SkeletonRows`：明滅。
  - `TabHighlight`：選択タブのオレンジピルをフェード＋拡大で切替。
  - `PressScale` は互換のための静的ラッパー（未使用）。
- 操作フィードバック：`m3.tsx` の `M3Touch` が押下で 0.96 に沈み込み、
  離すとスプリングで戻る（Reduce Motion 時はリップルのみ）。
- 画面遷移：ルート Stack は iOS/Android/Web いずれも右からスライド
  (`animation: 'slide_from_right'`)。左のメニューボタンから開く `menu` のみ
  左からスライド (`slide_from_left`)。Web はネイティブスタックが遷移
  アニメーションを持たないため `_layout.web.tsx` で `expo-router/js-stack`
  の JS スタックを使い、同アニメーションを実現する（ネイティブは
  `_layout.tsx`）。タブ切替は `animation: 'fade'`。
  Reduce Motion 時はすべて `animation: 'none'`。
- 依存：`react-native-reanimated`（4.x、`babel-preset-expo` が worklets
  プラグインを自動適用）。`@react-spring/native` は未使用のため使わない。


## 15. レスポンシブ（`context/responsive.tsx`＋`theme.ts`）

- 画面幅に応じて文字・余白・形状を一律に拡縮する。倍率は
  `scale = min(1, windowWidth / BASE_WIDTH)`（`BASE_WIDTH = 390`、**上限 1**。
  拡大は行わず、狭い画面のみ縮小する）。
- `ResponsiveProvider` を `src/app/_layout.tsx` でルート Stack に被せ、
  `useM3()` が `{ scale, type, layout, shape }` を返す。
  `type`/`layout`/`shape` は `scaleM3Type`/`scaleM3Layout`/`scaleM3Shape` で
  `m3type`/`m3layout`/`m3shape` を倍率適用したコピー。
- 各画面・`components/m3.tsx` は `useM3()` の `type`/`layout`/`shape` を参照し、
  `StyleSheet` の固定寸法は `scaled(n, scale)` で生成する（`createStyles(scale)` ＋
  `useStyles()`）。`borderWidth` とピル半径（999）は非スケール。
- `M3Icon` のサイズ、時間割の時間グリッド（ガター・1分あたりの高さ・時刻ラベル）も追従。
- `m3type`/`m3layout`/`m3shape` は基準値として引き続き export（`adminStyles`
  の互換用に静的版も残す）。
